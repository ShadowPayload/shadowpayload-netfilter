#ifndef _PAYLOAD_CRYPTO_H
#define _PAYLOAD_CRYPTO_H

enum {
	ENCRYPT_NETWORK,
	ENCRYPT_TRANSPORT,
	ENCRYPT_MAXIMUM,
	DECRYPT_NETWORK,
	DECRYPT_TRANSPORT
} crypto_option;

struct crypto_info {
};

#endif
